# WaterSense — Датчик уровня воды

**Wemos D1 Mini (ESP8266) + HC-SR04 — умный монитор уровня воды**

---

## Возможности

| Функция | Описание |
|---|---|
| 📏 Измерение | HC-SR04, усреднение N замеров |
| 🌐 Веб-интерфейс | Адаптивный, тёмная тема, график |
| 📊 История | Почасовые снимки, 7 дней (168 записей) |
| 📡 MQTT | Публикация level / volume / distance / json |
| ✈️ Telegram | Бот с командами + пороговые алерты |
| 🔄 OTA | ArduinoOTA + веб-загрузка /update |
| 📶 AP-режим | Первичная настройка без хардкода WiFi |
| 🕒 NTP | Реальное время для временных меток |
| 📥 Экспорт | CSV-выгрузка истории |
| 🔔 Алерты | Оповещения Telegram при низком/высоком уровне |
| 📅 Дневной отчёт | Telegram-сводка в полночь |

---

## Подключение HC-SR04

```
Wemos D1 Mini    HC-SR04
─────────────    ───────
5V         ──▶  VCC
GND        ──▶  GND
D5 (GPIO14)──▶  TRIG
D6 (GPIO12)──▶  ECHO
```

> **Важно:** Echo HC-SR04 выдаёт 5V! Используйте делитель напряжения:
> ECHO → R1(10кΩ) → D6, R2(20кΩ) → GND
> Или используйте HC-SR04 на 3.3V (вариант HC-SR04+).

---

## Установка (PlatformIO)

```bash
# Установить PlatformIO CLI
pip install platformio

# Клонировать / войти в папку
cd water-level-sensor

# Собрать и загрузить прошивку
pio run -t upload

# Загрузить файловую систему (web UI)
pio run -t uploadfs

# Мониторинг порта
pio device monitor
```

---

## Первый запуск

1. При первом включении (без настроек WiFi) устройство поднимает точку доступа:
   **SSID:** `WaterSensor-XXXXXX`  **Пароль:** `watersensor`
2. Подключитесь, откройте `http://192.168.4.1`
3. Перейдите в **Настройки**, введите параметры WiFi, сохраните
4. Устройство перезагрузится и подключится к вашей сети
5. Найдите IP в роутере или используйте `http://watersensor.local`

---

## Калибровка

- **Расстояние до дна** — поместите датчик, убедитесь что бочка пустая, измерьте рулеткой расстояние от датчика до дна
- **Расстояние полной бочки** — ≈ 3–5 см (зазор до максимального уровня воды)
- **Диаметр** — внутренний диаметр бочки в см (для расчёта литров)

---

## Веб-интерфейс

| URL | Описание |
|---|---|
| `/` | Дашборд с графиком и статусом |
| `/set.html` | Настройки |
| `/update` | Веб-OTA обновление прошивки |
| `/api/status` | JSON текущего состояния |
| `/api/history?h=24` | JSON истории (24/72/168 часов) |
| `/api/export` | Скачать историю CSV |
| `/api/measure` POST | Принудительный замер |
| `/api/config` GET/POST | Чтение/запись конфигурации |
| `/api/reset` POST | Сброс к заводским настройкам |

---

## MQTT топики

При базовом топике `home/water`:

| Топик | Значение |
|---|---|
| `home/water/level` | Уровень % |
| `home/water/distance` | Расстояние, см |
| `home/water/volume` | Объём воды, л |
| `home/water/free` | Свободный объём, л |
| `home/water/json` | JSON со всеми данными |

---

## Telegram-бот

Команды:
- `/level` или `/уровень` — текущий уровень
- `/status` или `/статус` — полный статус
- `/help` или `/помощь` — список команд

Алерты отправляются автоматически при выходе за настроенные пороги.

---

## Структура проекта

```
water-level-sensor/
├── platformio.ini
├── src/
│   ├── main.cpp            # Основной цикл
│   ├── config.h            # Конфиг, load/save
│   ├── sensor.h            # HC-SR04 драйвер
│   ├── storage.h           # Кольцевой буфер (LittleFS)
│   ├── webserver.h         # HTTP маршруты
│   ├── mqtt_handler.h      # MQTT клиент
│   └── telegram_handler.h  # Telegram бот
└── data/                   # LittleFS (веб-файлы)
    ├── index.html          # Дашборд
    ├── set.html            # Настройки
    ├── s.css               # Стили
    └── c.js                # Tiny chart (~4KB)
```
